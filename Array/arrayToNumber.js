const digits = [1, 2, 3, 4, 5];
const number = parseInt(digits.join(''), 10);
console.log(number); // Output: 12345

/* 
    const digits = [1, 2, 3, 4, 5];
    const number = Number(digits.join(''));
    console.log(number); // Output: 12345 
    
*/

/*
const digits = [1, 2, 3, 4, 5];
const number = digits.reduce((acc, curr) => acc * 10 + curr, 0);
console.log(number); // Output: 12345

*/

